import React from 'react'

import '../styles/Header.css'

const Header = () => {
  return(
    <header>
      <h1>c<span>(</span>v<span>)</span>reator<span>.</span></h1>
    </header>
  )
}

export default Header